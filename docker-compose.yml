version: '3'

networks:
  default:
    name: kittygram_final_default

volumes:
  pg_data:
  static:
  media:

services:
  db:
    image: postgres:13
    env_file: .env
    networks:
      - default
    volumes:
      - pg_data:/var/lib/postgresql/data
    restart: always
  backend:
    networks:
      - default
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/health/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    build: ./backend/
    env_file: .env
    volumes:
      - static:/app/static
      - media:/app/media
    depends_on:
      - db
    restart: always
    ports:
      - 9000:9000
  frontend:
    env_file: .env
    build: ./frontend/
    networks:
      - default
    volumes:
      - static:/app/static
    command: >
      sh -c "npm run build && cp -r /app/build/. /app/static/"
    restart: unless-stopped
  gateway:
    build: ./nginx/
    env_file: .env
    networks:
      - default
    ports:
      - 80:80
      - 443:443
    volumes:
      - static:/static
      - media:/media
    depends_on:
      - backend
      - frontend
    restart: always
